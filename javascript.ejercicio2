
//For
num = 10;
    for (var i = num - 1; i >= 1; i--) {
      num *= i;
    }
    console.log (num);

// while
var num = 10

        var result = num;
        if (num === 0 || num === 1) 
          return 1; 
        while (num > 1) { 
          num--;
          result *= num;
        }
        console.log(result);

